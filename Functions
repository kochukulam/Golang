package main

import (
	"fmt"
)

// Function takes two int as parameters.

func add(x int, y int) int {
	return x + y
}

// Main uses add function for adding the number

func main() {
	fmt.Println(add(20, 20))
}
